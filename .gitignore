# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# -----------------------------------------------
# !! SENSITIVE — never commit these !!
# -----------------------------------------------

# Environment variables (API keys, secrets, endpoints)
.env
.env.*
!.env.example

# AWS credentials / config
.aws/
aws-exports.js
aws-exports.ts
amplify/.config/local-*
amplify/backend/awscloudformation/

# SSL / TLS certificates and private keys
*.pem
*.key
*.crt
*.cert
*.p12
*.pfx

# Service-account / secrets files
secrets.json
secrets.yaml
secrets.yml
*secret*
*credentials*

# Backend Lambda zips (may contain bundled secrets)
*.zip

# Terraform / infra state (may contain sensitive output)
*.tfstate
*.tfstate.*
.terraform/
